is infeasible, we guide the search with a UCB-style [52] heuristic that balances exploitation and exploration while accounting for cost. For each node v, the algorithm maintains the cumulative reward V (v) (aggregated over historical visits) and the visit count N(v). We use Nparent(v) to denote the visit count of v’s parent node. We define the cost-aware utility as U(v) = V (v) N(v) + c s ln Nparent(v) N(v) −λ cost(Φv) B , (2) where c and λ are tuning parameters. This extends standard UCB to our budget-constrained setting. Algorithm Overview. As outlined in Algorithm 1, we ini- tialize the root node of the policy tree and iteratively refine the tree until the iteration budget is exhausted. Each iteration performs four phases: (1) Selection, traversing from root to an unexpanded node using node utility U(v); (2) Expansion, generating all possible child nodes within the cost budget; (3) Simulation, evaluating all children in parallel and assign- ing them node benefits W(Φv); and (4) Backpropagation, updating the cumulative reward V (·) and visit count N(·) along the path to the root using the obtained benefit W(Φv). After completing all iterations, we obtain the MCTS policy tree TMCT S and select the node with the maximum reranker- assigned benefit W(Φv) among all non-root nodes that satisfy the cost budget constraint. Notably, unlike traditional MCTS, which focuses on leaf nodes, our method considers nodes at all depths, as shorter combinations may outperform longer ones due to budget constraints or semantic coherence. Detailed Phases. We now describe the five phases of the MCTS-based policy tree search. (1) Selection (Function 1). Starting from the root, the algo- rithm recursively selects child nodes guided by the utility function U(v) (Eq. 2), which balances exploitation, explo- ration, and cost. At each step, among available children, we select the one with the highest U(v) value. The exploration Policy Tree Selection Expansion & Simulation Backpropagation Find Best Node W=0.3 V=0.3 N=1 W=0.2 V=0.2 N=1 W=0.7 V=0.7 N=1 Parallel Evaluate Potential Chunks χ1 χ2 χ4 χ1 R … χ4 χ1+χ2 χ1+χ3 χ1+χ4 … W=0.1 V=0.1 N=1 R … W=0.2 V=0.2 N=1 W=0.1 R … χ1+χ2 χ1+χ3 χ1+χ4 W=0.2 0.1 W=0.2 V=1.4 N=4 Root … W =0.3 W =0.7 W =0.2 Fig. 3: Workflow of MCTS-Based Policy Tree Search Algorithm 1: MCTS-Based Policy Tree Search Input: q: a query; D: a set of candidate chunks; B: total budget; Reranker: predicted reranker 1 Initialize root node v0 with query q for the policy tree TMCT S; 2 while within iteration do 3 vselected = Selection(v0, B); 4 {v1, . . . , vk} = Expansion(vselected, D, B); 5 Simulation(vselected, {v1, . . . , vk}, Reranker); 6 BackPropagation({v1, . . . , vk}); 7 return arg maxv∈TMCT S,cost(Φv)≤B W(Φv) term c p ln Nparent(v)/N(v) in U(v) ensures that less-visited nodes receive priority, preventing premature convergence to locally optimal paths. Selection proceeds until reaching an unexpanded node, ensuring both deep and broad exploration of the search space. (2) Expansion (Function 2). Upon reaching
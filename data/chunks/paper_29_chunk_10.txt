vk}); 7 return arg maxv∈TMCT S,cost(Φv)≤B W(Φv) term c p ln Nparent(v)/N(v) in U(v) ensures that less-visited nodes receive priority, preventing premature convergence to locally optimal paths. Selection proceeds until reaching an unexpanded node, ensuring both deep and broad exploration of the search space. (2) Expansion (Function 2). Upon reaching an unexpanded node v, all potential child nodes {v1, v2, . . . , vk} are generated by appending one additional chunk (each child cost does not exceed the budget B) to the current combination. Unlike standard MCTS, which expands a single child per iteration, our approach expands all possible children simultaneously to prepare for parallel evaluation. In particular, we initialize each new node’s cumulative reward and visit count to zero. (3) Simulation: Parallel Evaluation (Function 3). Instead of random rollouts, we evaluate all expanded nodes in parallel using the reranker [W(Φv1), . . . , W(Φvk)] = Reranker(Q, [Φv1, . . . , Φvk]), where W(Φvj) denotes the reranker-assigned benefit for combination Φvj. This design allows all k combinations to be scored in one model call (cost O(r)) rather than k separate calls (cost O(k · r)), leveraging transformer rerankers’ batch processing for substantial effi- ciency gains. For each vj, we record W(Φvj) and compute its token cost cost(Φvj). (4) Backpropagation (Function 4). Each child’s benefit W(Φvj) is propagated upward along the path to the root, updating cumulative rewards V (·) and visit counts N(·). Unlike traditional MCTS, which updates only the best path, we backpropagate all children independently, preserving richer exploration statistics to guide future iterations. (5) Final Node Selection. After all iterations, we perform a global search across explored nodes and select the one with the highest benefit under the cost budget: ˆv = arg maxv∈TMCT S, cost(Φv)≤B W(Φv). Unlike traditional MCTS methods, the final decision is based on the reranker- Function 1: Selection Input: vi: current tree node; B: total budget 1 while vi.isFullyExpanded() do 2 foreach child nodes vj of vi do 3 compute utility U(vj) = V (vj) N(vj) + c r ln Nparent(vj ) N(vj) −λ cost(Φvj ) B ; 4 vi = arg maxvj∈Children(vi) U(vj); 5 return vi Function 2: Expansion Input: v: unexpanded node to expand; D: set of candidate chunks; B: total budget // Create all possible child nodes 1 Expand v by using the candidate chunks in D to construct child nodes {v1, v2, . . . , vk} whose costs do not exceed the budget B; 2 foreach child nodes vj do 3 calculate and record cost(Φvj); 4 initialize N(vj) = 0, V (vj) = 0; 5 return {v1, v2, . . . , vk} Function 3: Simulation Input: v: parent node; {v1, . . . , vk}: expanded children; Reranker: predicted reranker // Parallel evaluation via reranker 1 compute benefit [W(Φv1), . . . , W(Φvk)] = Reranker(Q, [Φv1, . . . , Φvk]); 2 foreach child nodes vj do 3 record W(Φvj); Function 4: BackPropagation Input: {v1, v2, . . . , vk}: the expanded child
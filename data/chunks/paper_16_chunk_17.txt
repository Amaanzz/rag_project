childhood memory? Do you have a special story to share? 16 Enhancing Human-Like Responses in Large Language Models E Training Techniques in Detail E.1 LoRA (Low-Rank Adaptation) LoRA [12] utilizes low-rank approximations for fine-tuning large-scale models, which reduces computational and memory overhead while preserving the model’s structure and mitigating overfitting. Pretrained Weights 𝑊∈ℝ𝑑×𝑑 x h 𝐵= 0 𝐴= 𝒩(0, 𝜎2) 𝑑 𝑟 Figure 6: LoRA technique Low-Rank Approximation A matrix W ∈Rd×k with rank r can be approximated by A ∈Rr×k and B ∈Rd×r: W ≈A × B Model Fine-Tuning LoRA introduces a low-rank update ∆W: W ′ = W + ∆W with ∆W = A × B The output for input x is: h = W0x + ∆Wx = W0x + BAx During fine-tuning, only A and B are updated. E.2 DPO (Direct Preference Optimization) Direct Preference Optimization (DPO) [17] is a method aimed at optimizing a model’s preferences directly based on preferences. It utilizes the preferences among alternative outputs in a specific situation. • Reference Policy πref(y|x): Represents the probability distribution of the output y given the state x. • Reward Function r(x, y): Measures the level of reward for a given state x and the output y. It is defined as: Z(x) = X y πref(y|x) exp  1 β r(x, y)  r(x, y) = β log πr(y | x) πref(y | x) + β log (Z(x)) where β is the temperature parameter that adjusts the impact of the reward function on the outputs. • Optimization Objective: The goal is to optimize the following equation to align the model’s outputs with human preferences: πr(y|x) = πref(y|x) exp  1 β r(x, y)  Z(x) This equation aims to ensure that the learned model provides outputs that align with human preferences. The model optimizes the objective by minimizing the corresponding loss function, thereby improving performance in a manner consistent with human judgment. 17